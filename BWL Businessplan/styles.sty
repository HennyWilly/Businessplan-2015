\ProvidesPackage{styles}%

% Includes
\RequirePackage[ngerman]{babel}% Deutsche Styles
\RequirePackage[ansinew]{inputenc}% Input-Formatierung
\RequirePackage[T1]{fontenc}% Ausgabe-Formatierung
\RequirePackage{ngerman} %Umlaute
\RequirePackage{color}% Farbige Texte
\RequirePackage{ifthen}% If-Else
\RequirePackage{graphicx}% Bilder einf�gen
\RequirePackage{hyperref}% Hyperlinks im Inhaltsverzeichnis und Fu�noten
\RequirePackage[gen]{eurosym} %Eurozeichen \euro
\RequirePackage{float} %Feste Platzierung von Tabellen

% Funktionen
\newcommand{\kursiv}[1]{\textit{#1}}%
\newcommand{\boldText}[1]{\textbf{#1}}%
\newcommand{\missingText}[1]{\textcolor{red}{\boldText{#1}}}%
\newcommand{\missingFootnote}[1]{\footnote{\missingText{#1}}}%
\newcommand{\tryincludegraphics}[2][]{%
	\IfFileExists{#2}{%
		\includegraphics[#1]{#2}%
	}%
	%else
	{%
		\fbox{\missingText{Hier k\"onnte Ihr Bild '#2' stehen!}}%
	}%
}%

% Konstanten
\newcommand{\firmenname}{CaffeMac}%
\newcommand{\produktname}{\missingText{Produktname}}%